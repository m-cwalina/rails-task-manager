<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %>:
    <%=link_to "🗑", task_path(task.id), method: :delete %></li>
    <%=link_to "✍️", edit_task_path(task.id), method: :get %></li>
  <% end %>
</ul>

<p><%= link_to "Add a task", new_task_path %></p>
